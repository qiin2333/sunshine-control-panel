{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-pet-screenshot",
  "description": "Allows only the bundled desktop pet toolbar to capture the primary display.",
  "windows": ["toolbar"],
  "local": true,
  "permissions": [
    "allow-capture-screenshot"
  ]
}
