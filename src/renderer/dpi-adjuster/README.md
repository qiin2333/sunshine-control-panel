# DPI 调整器

## 功能说明

这是一个独立的 DPI 调整窗口，用于快速调整 Windows 桌面的显示缩放比例。

## 特性

- 🎚️ **滑动条调整** - 100% 到 300% 范围
- 🔢 **预设值** - 常用 DPI 值一键选择
- 🎨 **现代界面** - 渐变背景，动画效果
- ⚡ **实时反馈** - 应用结果即时显示

## 使用方法

### 1. 打开 DPI 调整器

**方式 A**: 从工具栏
- 右键点击工具栏图标
- 选择 "调整 DPI"

**方式 B**: 代码调用
```javascript
// 触发菜单事件
handle_toolbar_menu_event(app, "toolbar_dpi");
```

### 2. 调整 DPI

**滑动条调整**:
- 拖动滑块选择 DPI 值
- 步进: 25%
- 范围: 100% - 300%

**预设值选择**:
- 点击预设按钮快速选择
- 可用预设: 100%, 125%, 150%, 175%, 200%, 225%, 250%, 300%

### 3. 应用设置

点击 "应用 DPI" 按钮，系统将调用 `SetDpi.exe` 设置新的缩放比例。

## 技术实现

### 后端 (Rust)

```rust
#[tauri::command]
async fn set_desktop_dpi(dpi: u32) -> Result<(), String>
```

**工作流程**:
1. 获取 Sunshine 安装路径
2. 定位 `SetDpi.exe` (在 `tools/` 目录)
3. 执行命令: `SetDpi.exe <dpi>`
4. 返回执行结果

### 前端 (Vue 3)

**组件**: `DpiAdjusterApp.vue`

**功能**:
- 响应式 DPI 值绑定
- 滑动条和预设按钮
- 应用状态和错误处理
- 成功/失败消息提示

### 窗口配置

```rust
.title("DPI 调整")
.inner_size(400.0, 300.0)
.resizable(false)
.decorations(true)
.center()
```

## SetDpi.exe 要求

### 位置
```
<Sunshine 安装目录>/tools/SetDpi.exe
```

### 参数
```bash
SetDpi.exe <DPI值>
```

例如:
```bash
SetDpi.exe 150  # 设置为 150%
SetDpi.exe 200  # 设置为 200%
```

## 界面设计

### 布局
```
┌──────────────────────────┐
│    🖥️ 调整桌面 DPI        │ 标题
│  拖动滑块调整显示缩放比例   │
├──────────────────────────┤
│                          │
│        150%              │ 大号 DPI 显示
│                          │
│    ━━━━●━━━━━━━━━━       │ 滑动条
│  100  150  200  250  300 │
│                          │
│  [100] [125] [150] ...   │ 预设按钮
│                          │
│    【 应用 DPI 】         │ 应用按钮
│                          │
│  ✅ DPI 已设置为 150%     │ 状态消息
└──────────────────────────┘
```

### 配色
- 主色: 蓝紫色渐变 (#667eea → #764ba2)
- 按钮: 白色
- 文字: 白色
- 强调: 渐变背景

### 动画
- DPI 数值: 脉冲呼吸效果
- 按钮: 悬停上浮
- 消息: 滑入动画

## 常见问题

### Q: 找不到 SetDpi.exe
**A**: 确保 Sunshine 已正确安装，并且 `tools/SetDpi.exe` 存在。

### Q: DPI 设置后没有生效
**A**: 部分应用需要重启才能应用新的 DPI 设置。

### Q: 权限不足
**A**: 某些系统可能需要管理员权限来更改 DPI 设置。

## 开发

### 本地测试
```bash
npm run dev
```

### 构建
```bash
npm run build:renderer
```

### 添加到构建
已在 `vite.config.js` 中配置:
```javascript
dpiAdjuster: resolve(rendererSrcPath, 'dpi-adjuster/index.html')
```

## 文件结构

```
dpi-adjuster/
├── index.html          # HTML 入口
├── main.js             # Vue 应用入口
├── DpiAdjusterApp.vue  # 主组件
└── README.md          # 本文档
```

