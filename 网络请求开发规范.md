# Sunshine Control Panel 网络请求开发规范

本文档说明 Sunshine Control Panel 中网络请求的架构设计、踩坑记录及开发规范，适用于所有需要从外部服务器获取资源的场景。

---

## 一、背景：WebView 的网络限制

Sunshine Control Panel 是基于 Tauri v2 构建的桌面应用，UI 运行在 WebView2（Windows）内。理解两个关键限制：

### 1.1 CORS（跨域资源共享）

WebView 页面的 origin 是 `http://tauri.localhost`，当 JavaScript 通过 `fetch()` 或 `XMLHttpRequest` 请求外部 URL 时，**浏览器会检查响应头中的 `Access-Control-Allow-Origin`**。

- 如果目标服务器未返回 CORS 头 → 浏览器**直接丢弃响应**，JS 拿不到任何数据
- 这是浏览器内核层面的安全策略，**无法通过前端代码绕过**
- `tauri.conf.json` 的 `remote.urls` 控制的是远程页面能否调用 Tauri IPC API，**与 CORS 无关**

### 1.2 TLS 兼容性

Rust 的 HTTP 客户端库 reqwest 支持两种 TLS 后端：

| 后端 | 实现 | Windows 表现 |
|------|------|-------------|
| `rustls-tls` | 纯 Rust 实现 | 少数 CDN/服务器握手不兼容（连接重置） |
| `native-tls` | 调用系统库（Windows=SChannel） | 与 Chrome/Edge 相同的 TLS 栈，兼容性最好 |

---

## 二、案例：桌宠 CDN 资源加载失败

### 2.1 问题现象

某功能需要从下载资源：

在客户端机器上运行时，资源加载失败。

### 2.2 根因分析（共计三个独立问题）

#### 问题 1：TLS 握手被 CDN 重置

```
error sending request for url: connection closed before message completed
os error 10054: 远程主机强迫关闭了一个现有的连接
```

**根因**：`reqwest` 使用 `rustls-tls`，该 CDN 的 TLS 配置不兼容 rustls 的握手方式。

**修复**：`Cargo.toml` 中将 `rustls-tls` 改为 `native-tls`，使用 Windows SChannel 实现 TLS。

#### 问题 2：WebView CORS 拦截

```
TypeError: Failed to fetch（浏览器控制台）
```

**根因**：WebView origin 是 `http://tauri.localhost`，CDN 响应头无 `Access-Control-Allow-Origin`。浏览器按 CORS 策略拦截了响应。

**修复**：新增 Tauri command `fetch_remote_bytes`，在 Rust 后端通过 reqwest 下载（不受 CORS 限制），将数据以 base64 data URL 返回给前端。

#### 问题 3：UTF-8 BOM 导致 JSON 解析失败

```
JSON 解析失败: expected value at line 1 column 1
响应体前200字符: ﻿[
```

**根因**：CDN 返回的 JSON 文件带 UTF-8 BOM（`\uFEFF`），`serde_json` 不接受 BOM 前缀。

**修复**：在解析前 `trim_start_matches('\u{FEFF}')`。

### 2.3 附加优化

| 优化 | 说明 |
|------|------|
| 共享连接池 | `cdn_client()` 使用 `OnceLock<reqwest::Client>` 实现单例，避免每次请求创建新 Client 和重复 TLS 握手 |
| 自动重试 | `cdn_get()` 网络失败时等 1s 重试一次 |
| gzip/deflate | reqwest 开启压缩解码，减少传输量 |
| ETag 条件请求 | `cdn_get()` 支持 `If-None-Match` 头，服务端返回 304 时不传输数据，节省带宽 |
| IndexedDB 缓存 | 精灵图和话术缓存到 IndexedDB（含 ETag），启动时立即使用缓存，后台条件请求验证更新 |
| 定时刷新 | 每 30 分钟发送 ETag 条件请求，无变化时流量极小 |
| Canvas Fallback | 所有网络方式失败时，前端用 Canvas 生成简笔画 fallback |

---

## 三、网络请求架构规范

### 3.1 决策流程图

当需要在前端获取外部资源时：

```
目标服务器是否支持 CORS？
  │
  ├─ 是 → 前端直接 fetch()
  │
  └─ 否（或不确定）→ 通过 Rust 后端代理
                        │
                        ├─ JSON 类 → 专用 Tauri command（可做 BOM/格式处理）
                        └─ 二进制类 → fetch_remote_bytes（返回 data URL）
```

### 3.2 Rust 代理模式

**架构：**

```
前端 (WebView)                              Rust 后端
  │                                           │
  │  invoke('fetch_remote_bytes', {url})      │
  │ ─────────────────────────────────────────→ │
  │                                           │  cdn_get(url) → reqwest GET → CDN
  │                                           │  ← HTTP 200 + bytes
  │                                           │  base64 encode
  │  ← data:{mime};base64,{encoded}           │
  │                                           │
  │  (前端可以用 data URL 作为 img.src         │
  │   或 fetch(dataUrl).blob() 获取 Blob)     │
```

**优势：**

| 优势 | 说明 |
|------|------|
| 绕过 CORS | Rust HTTP 请求不受浏览器同源策略限制 |
| TLS 兼容性 | 使用操作系统 TLS 栈，兼容性好 |
| 安全可控 | 白名单限制可代理的域名，防止任意 URL 代理 |
| 连接池复用 | 共享 Client 避免重复握手 |
| 重试能力 | 后端可实现重试逻辑，前端无感 |
| BOM/编码处理 | 后端可处理各种编码问题再返回干净数据 |

**劣势：**

| 劣势 | 说明 |
|------|------|
| 内存开销 | 二进制资源需 base64 编码传输，体积增大约 33% |
| 不适合大文件 | data URL 全量加载到内存，不适合超过几 MB 的文件 |
| 额外复杂度 | 需要维护 Rust 端代理命令和白名单 |
| 无流式传输 | 不支持边下载边使用（如流媒体），只能全量返回 |

**适用场景：**

- ✅ 图片、JSON、CSS 等小型静态资源（< 5MB）
- ✅ CDN 无 CORS 头的第三方资源
- ❌ 大文件下载（应使用 Tauri 的文件下载 API 或 streaming 方案）
- ❌ WebSocket / SSE 等长连接

### 3.3 安全规范

1. **始终使用域名白名单**：`fetch_remote_bytes` 只允许已知安全域名
2. **仅 HTTPS**：白名单应以 `https://` 开头
3. **白名单格式**：以 `/` 结尾（如 `https://example.com/`），防止域名前缀攻击
4. **新增域名需评审**：添加新的可代理域名时需要安全评审

### 3.4 TLS 后端选择规范

- **默认使用 `native-tls`**：与操作系统 TLS 栈一致，兼容性最好
- **`rustls-tls` 适用于**：纯 Rust 编译（如交叉编译到没有系统 TLS 的环境）
- **不要同时启用两者**：在 `Cargo.toml` 中 reqwest 只启用一种 TLS feature

### 3.5 连接管理规范

- **使用共享 Client**：不要每次请求都 `reqwest::get()`（等于每次创建新 Client）
- **推荐模式**：`OnceLock<reqwest::Client>` 或 Tauri 的 State 管理
- **配置连接池**：`pool_max_idle_per_host(5)` + 合理的超时时间
- **设置超时**：总超时 15s，连接超时 10s（根据场景调整）

### 3.6 错误处理规范

- **前端**：网络失败不应阻塞 UI，always 有 fallback 或默认值
- **后端**：使用 `warn!()` 记录失败（不要用 `debug!()`，Release 看不到）
- **重试**：网络请求至少 1 次重试，间隔 1s
- **UTF-8 BOM**：解析外部 JSON 时始终 `trim_start_matches('\u{FEFF}')`

---

## 四、常见陷阱

### 4.1 `tauri.conf.json` 的 `remote.urls` ≠ CORS 白名单

```json
{
  "security": {
    "capabilities": [{
      "remote": {
        "urls": ["https://*.example.com/*"]
      }
    }]
  }
}
```

这个配置控制的是：**从这些远程 URL 加载的页面能否调用 Tauri IPC API**。它不会影响 WebView 的 CORS 策略。

### 4.2 `reqwest::get()` 每次创建新 Client

```rust
// ❌ 错误：每次调用创建新 Client，频繁握手可能被 CDN 限流
let resp = reqwest::get(url).await?;

// ✅ 正确：共享 Client，复用连接和 TLS 会话
let client = cdn_client(); // OnceLock 单例
let resp = client.get(url).send().await?;
```

### 4.3 CDN 返回的 JSON 可能带 BOM

UTF-8 BOM（`\uFEFF`，3 字节 `EF BB BF`）在很多编辑器和 CDN 中是常见的。`serde_json` 和大多数 JSON 解析器不认 BOM。

```rust
// ✅ 安全的 JSON 解析
let text = response.text().await?;
let text = text.trim_start_matches('\u{FEFF}');
let data: T = serde_json::from_str(text)?;
```

### 4.4 PixiJS Assets.load 遵循 CORS

`PIXI.Assets.load(url)` 内部用 `fetch()` 加载资源，同样受 CORS 限制。在无 CORS 头的情况下不能依赖它加载跨域图片。

---

## 五、Checklist

新增外部网络请求时，按此清单检查：

- [ ] 目标服务器是否支持 CORS？如果不确定，使用 Rust 代理
- [ ] 使用共享的 `reqwest::Client`，不要 `reqwest::get()`
- [ ] reqwest 使用 `native-tls` 后端
- [ ] 有失败重试逻辑（至少 1 次）
- [ ] JSON 解析前去除 BOM
- [ ] 代理命令有域名白名单
- [ ] 前端有 fallback / 默认值，网络失败不阻塞 UI
- [ ] 错误日志用 `warn!()` 或 `info!()`（Release 可见），成功日志用 `info!()`
- [ ] 资源大小合理（< 5MB），大文件用其他方案
